import React from 'react'
import styles from './styles.scss'

const Pick = () =>
  <div className={`${styles.pick}`}>
    <select>
      <option value="">
        {'--'}
      </option>
    </select>
  </div>

export default Pick
